version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - copyloopvar
    - gocritic
    - misspell
    - noctx
    - revive

formatters:
  enable:
    - gofmt
